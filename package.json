{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-chrome": "npx playwright test --browser=chromium",
    "test-firefox": "npx playwright test --browser=firefox",
    "test-webkit": "npx playwright test --browser=webkit",
    "smoke-chrome": "npx playwright test --project=smoke-chrome",
    "smoke-chrome-branded": "npx playwright test tests/smoke --browser=chromium --channel=chrome",
    "regression-chrome": "npx playwright test --project=regression-chrome",
    "regression-firefox": "npx playwright test --project=regression-firefox",
    "regression-safari": "npx playwright test --project=regression-safari",
    "test-workers": "npx playwright test --workers=3",
    "smoke-workers": "npx playwright test tests/smoke --workers=3",
    "regression-workers": "npx playwright test tests/regression --workers=3",
    "smoke-chrome-workers": "npx playwright test --project=smoke-chrome --workers=3",
    "regression-chrome-workers": "npx playwright test --project=regression-chrome --workers=3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3"
  }
}
